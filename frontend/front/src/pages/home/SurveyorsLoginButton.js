import { Box, Button, Typography } from "@mui/material";
import React from "react";

const SurveyorsLoginButton = () => {
  return (
    <Box p={3} m={2}>
      <Button>
        <Typography variant='h2'>Surveyor Login</Typography>
      </Button>
    </Box>
  );
};

export default SurveyorsLoginButton;
