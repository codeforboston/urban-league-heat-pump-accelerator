<%= form_with(model: survey_visit) do |form| %>
  <% if survey_visit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(survey_visit.errors.count, "error") %> prohibited this survey_visit from being saved:</h2>

      <ul>
        <% survey_visit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :completed, style: "display: block" %>
    <%= form.check_box :completed %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
